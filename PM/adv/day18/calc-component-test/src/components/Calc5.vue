<template>
  <div>
    X : <input type="text" v-model.number="x" /> <br />
    결과 : {{ result }}
  </div>
</template>

<script>
import { ref, watch } from 'vue';

export default {
  name: 'Calc5',
  setup() {
    const x = ref(10);
    const result = ref(0);

    watch(x, (c, o) => {
      console.log(`${o} -> ${c}`);
      result.value = c * 2;
    });

    return { x, result };
  },
};
</script>
